// middleware/auth.global.ts
export default defineNuxtRouteMiddleware(async (to, from) => {
    // const user = await getCurrentUser()
    
    // console.log(user)
    // // ...
    // if (!user && to.path !== '/' && to.path !== '/login' && to.path !== '/signup' ) {
    //   console.log('ve homepage')
    //   return navigateTo({ path: '/' })
    // }
  })